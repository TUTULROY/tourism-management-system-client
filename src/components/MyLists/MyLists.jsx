

const MyLists = () => {
//  const {_id, tourists_spot_name, country_Name, location, description, average_cost, photo}=item;
//  console.log(item);
    return (
        <div>
            <h1>hello</h1>
        </div>
    );
};

export default MyLists;